# V1.4 发版说明

发布日期：2026-08-03
版本代号：V1.4

## 本版范围

本次版本在保留原有浏览器本地优先打卡与卡片奖励逻辑的基础上，新增低成本云端同步、家长只读查看与兑换码关联机制。小朋友端以“兑换奖励”完成首次关联，后续打卡在后台静默同步；家长端通过家庭账号查看记录并管理兑换码。

## 本版完成项

### 1. 云端同步与本地优先

- 保留现有 `localStorage` 作为打卡、奖励与卡片收藏的第一存储位置。
- 每次打卡仍先保存到本机；已完成兑换的设备会自动将历史和后续打卡快照同步至 Supabase。
- 断网或云端暂时不可用时不影响打卡与奖励，本地记录保留并在后续联网时补同步。
- 每日同步采用“每天一条记录”的快照结构，包含日期、任务明细、完成状态、已完成数、总任务数、是否满进度与同步时间。

### 2. 小朋友端兑换码领奖

- 小朋友端不展示家庭账号、云端、绑定、同步、家长设置等技术或管理概念。
- 新增“输入兑换码”入口；点击后弹出独立兑换对话框。
- 对话框支持：自动聚焦、6 位数字校验、Enter 提交、兑换中禁用重复点击、弹窗内错误提示。
- 首次兑换成功后发放一次奖励：
  - R 卡：60%
  - SR 卡：20%
  - SSR 卡：5%
  - 3 星星：10%
  - 1 星星：5%
- 兑换成功自动关闭兑换对话框，并展示奖励弹窗；后续不重复显示兑换入口或重复发奖。
- 兑换奖励与云端结果使用同一张卡片 ID，修复了云端记录、奖励弹窗和本地卡片库可能出现不同卡片的问题。

### 3. 家长查看页

- 新增 `家长查看.html`，使用 Supabase Authentication 中创建的家庭账号登录。
- 页面仅提供查看能力，不修改小朋友的打卡内容。
- 支持查看：
  - 今天的完成进度；
  - 最近 7 天完成率；
  - 最近一次同步时间；
  - 每天的完成数与满进度状态；
  - 已同步日期的任务明细与完成状态。
- 新增“设备兑换码”管理：家长可设置或更换 6 位数字兑换码；更换仅影响后续新设备兑换，已兑换设备不受影响。

### 4. Supabase 服务端接入

- 新增匿名设备身份，用于小朋友端无需输入家庭账号即可完成兑换与后续同步。
- 新增 `family_groups`、`family_devices` 与 `daily_checkins` 数据结构及行级权限策略。
- 部署 `family-sync` Edge Function，承担：
  - 家长兑换码设置与状态读取；
  - 小朋友端兑换码校验；
  - 兑换奖励与匿名设备关联；
  - 打卡快照写入。
- 已启用 Anonymous Sign-Ins、执行建表脚本并部署云端函数。

## 涉及文件

- `每日打卡.html`
- `家长查看.html`
- `cloud-config.js`
- `cloud-sync.js`
- `supabase-schema.sql`
- `supabase/functions/family-sync/index.ts`
- `兑换码云端部署说明.md`
- `V1.4-发版说明.md`

## 验收建议

1. 用家庭账号登录 `家长查看.html`，设置 6 位兑换码。
2. 使用无痕窗口或新设备环境打开 `每日打卡.html`，点击“输入兑换码”。
3. 确认兑换输入以弹窗展示，输入框自动聚焦；输入兑换码后可点击或按 Enter 兑换。
4. 确认仅首次兑换出现奖励，刷新页面后不重复发奖。
5. 兑换后勾选一项任务，回到家长页点击“刷新”，确认当天进度与任务明细正确显示。
6. 暂时断网勾选任务，确认本地打卡不受影响；恢复网络后重新打开页面，确认记录可补同步。
7. 检查抽到卡片的奖励：奖励弹窗、卡片库中的新增卡与云端奖励结果应为同一张卡。

## 自检结果

- `每日打卡.html`、`家长查看.html` 内联脚本语法检查通过。
- `cloud-config.js`、`cloud-sync.js` JavaScript 语法检查通过。
- `check_release.py` 通过：26 张卡片、26 个 WebP 资源、共享卡片模块与 V1.3 本地数据迁移检查正常。
- Git 差异格式检查通过。
- Supabase Anonymous Sign-Ins 可用；匿名设备可调用 `family-sync` 的设备状态接口。
- 用户已完成真实流程联调：家长端设码、小朋友端兑换领奖、勾选任务、家长端查看同步数据均已走通。

## 已知边界

- 当前面向单个家庭、单台 iPad 使用。若清除该设备的网站数据，会生成新的匿名设备身份，需重新输入兑换码，并可能再次触发兑换奖励；后续如需严格防重，可增加设备管理或兑换码次数限制。
- 同一天若由多个设备同时同步，当前采用最后一次写入结果；单 iPad 场景不受影响。

## 建议提交信息

`release: 发布 V1.4`

## 发版结论

V1.4 已完成云端同步、兑换码奖励、家长查看及真实流程联调，可提交并推送至 GitHub Pages 发布。
