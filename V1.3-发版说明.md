# V1.3 发版说明

发布日期：2026-07-31  
版本代号：V1.3

## 本版范围

本次版本聚焦暑假后半程的课表适配与卡片收藏体验细节调整，不改变卡片奖励概率、解锁价格或浏览器本地存储结构。

## 本版完成项

### 1. 8 月周末课表调整

- 2026 年 8 月的周六、周日不再使用“周末减量”课表。
- 8 月周末统一按常规计划展示任务。
- 英语句子/课文抄写保持每周三次：周二、周四、周六。

### 2. 课程期间课表优化

- 数学暑期课调整至晚上。
- 英语课后作业调整至下午。
- 数学课后作业调整至晚上，并跟随数学暑期课安排。
- 课程期间周六：
  - 不安排数学暑期课、数学课后作业、数感训练；
  - 增加“53天天练”和“《一本》三年级生词”。
- 课程期间周日：
  - 使用常规课表；
  - 不安排英语课后作业和数学课后作业。

### 3. 卡片收藏入口视觉统一

- 首页“已获得卡片”统计入口右上角图标由 🃏 统一为 📔。
- “我的卡片库”标题左上角图标同步统一为 📔。
- 收藏册语义在首页入口与卡片库页面保持一致。

### 4. 满进度奖励弹窗收口

- 今日进度达到 100% 时，满进度奖励弹窗不再显示“继续打卡”按钮。
- 弹窗仍支持右上角关闭按钮与点击遮罩关闭，避免完成当日任务后出现无意义操作。

### 5. 工程维护与资源治理

- 新增 `cards-data.js`，作为 26 张正式卡片、卡图映射、稀有度样式工具和 SVG 回退逻辑的唯一来源。
- `每日打卡.html` 与 `全部卡片预览.html` 均接入共享卡片模块，保留各自的页面交互逻辑。
- 新增 `check_release.py` 发布检查：校验卡池、卡图资源、页面接入、版本与本地数据迁移标记。
- 新增 `未引用卡图资源审计报告.md`：扫描 84 个 PNG/WebP，正式引用 26 张 WebP，记录 58 张备用素材；未删除、移动或覆盖任何图片。

### 6. 缺陷修复（共享模块回归）

- 修复因抽取 `cards-data.js` 导致的回归：`getCard()` 在主页面中被大量使用，但抽取后未被暴露到 `window.CARD_DATA`，也未在主页面重新定义，造成 `getCard is not defined`。
- 该错误会在加载已有打卡/掉卡/兑换记录时中断 `renderAll()`，表现为：日历与待办不渲染、“回到今天”按钮失效、当前周（如 7/27–8/2）看不到数据。
- 已在 `cards-data.js` 暴露 `getCard`，并在 `每日打卡.html` 解构引入。
- 同步在 `normalizeStreakRewards` 中丢弃指向已不存在卡牌的旧掉落记录，避免任何历史脏数据再次中断整页渲染。

## 涉及文件

- `每日打卡.html`
- `全部卡片预览.html`
- `cards-data.js`
- `check_release.py`
- `未引用卡图资源审计报告.md`
- `V1.3-发版说明.md`

## 验收建议

1. 打开 `每日打卡.html`，确认顶部版本号显示 `V1.3`。
2. 选择 8 月周末日期，确认使用常规课表。
3. 选择课程期间的工作日，确认英语课后作业在下午、数学课后作业在晚上。
4. 选择课程期间周六，确认没有数学课后作业，并有“53天天练”“《一本》三年级生词”。
5. 选择课程期间周日，确认没有英语课后作业、数学课后作业。
6. 点击“已获得卡片”进入“我的卡片库”，确认两处均显示 📔 收藏册图标。
7. 完成某日全部任务，确认 100% 奖励弹窗不显示“继续打卡”按钮。
8. 运行发布检查：`python check_release.py`。
9. 加载已有打卡/掉卡/兑换记录，确认日历与待办正常渲染，点击“回到今天”可回到当前周。

## 自检结果

- 发布检查通过：26 张卡片、26 个 WebP 资源、两页共享模块接入、V1.3 与 localStorage 迁移检查通过。
- 桩 DOM 回归验证：含真实风格打卡/掉卡/兑换记录时，`renderAll()`、`jumpToToday()`、`selectDate()`、`changeWeek()` 均正常；含指向已不存在卡牌的旧掉落记录时不再崩溃。
- `cards-data.js` 语法检查通过。
- `check_release.py` Python 编译检查通过。
- `每日打卡.html` 与 `全部卡片预览.html` 内联脚本语法检查通过。

## 建议提交信息

`release: 发布 V1.3`

## Git 推送指令

请在真实 Git 仓库根目录执行。项目路径：

`C:\Users\sPark\WorkBuddy\2026-07-22-15-55-39`

### 方式一：Git Bash

```bash
cd "/c/Users/sPark/WorkBuddy/2026-07-22-15-55-39"
git status
git add "每日打卡.html" "全部卡片预览.html" "cards-data.js" "check_release.py" "未引用卡图资源审计报告.md" "V1.3-发版说明.md"
git commit -m "release: 发布 V1.3"
git push origin main
```

若 `git branch --show-current` 显示的不是 `main`，将最后一行的 `main` 替换为实际分支名。

### 方式二：PowerShell

```powershell
Set-Location "C:\Users\sPark\WorkBuddy\2026-07-22-15-55-39"
git status
git add "每日打卡.html" "全部卡片预览.html" "cards-data.js" "check_release.py" "未引用卡图资源审计报告.md" "V1.3-发版说明.md"
git commit -m "release: 发布 V1.3"
git push origin main
```

### 方式三：Windows 命令提示符（CMD）

```cmd
cd /d "C:\Users\sPark\WorkBuddy\2026-07-22-15-55-39"
git status
git add "每日打卡.html" "全部卡片预览.html" "cards-data.js" "check_release.py" "未引用卡图资源审计报告.md" "V1.3-发版说明.md"
git commit -m "release: 发布 V1.3"
git push origin main
```

若当前分支不是 `main`，先执行：

```powershell
git branch --show-current
```

然后将推送命令的 `main` 替换为实际分支名。

## 发版结论

V1.3 已完成文档整理、课表与交互调整，并完成发布前基础自检。执行上述 Git 提交与推送命令后，即可发布到远程仓库。
