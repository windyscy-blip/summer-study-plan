# V1.2 发版说明

发布日期：2026-07-28
版本代号：V1.2

## 本版范围
本次版本聚焦两块：
1. 卡片查看体验收口
2. 全部卡面预览页访问控制

## 本版完成项

### 1. 全部卡面预览页增加口令访问
- `全部卡片预览.html` 进入后默认显示深色蒙版
- 需输入口令后才可继续查看全部卡面
- 默认口令：`654321`
- 每次重新进入页面都需要重新输入
- 刷新页面后也必须重新输入
- 支持回车提交
- 输错后显示错误提示
- 弹窗新增“返回每日打卡”按钮，避免卡在口令页
- 弹窗文案收敛为极简形式，不展示默认口令，也不展示冗长说明文案

### 2. 全部卡面入口改为独立页面跳转
- `每日打卡.html` 中“查看全部卡面”入口，已从主页面内嵌 catalog 视图改为直接跳转 `全部卡片预览.html`
- 这样从入口进入时会正确触发口令蒙版，而不会绕过口令逻辑

### 3. 卡片详情展示修复
- 修复详情弹窗左侧卡面底部文案被边框遮挡的问题
- 为详情场景单独增加 `card-context-detail` 样式
- 收紧 detail 卡底部留白，上移 footer，保证底部文案完整可见

### 4. SR 卡“琉璃流星”重绘替换
- 对 `sr-glass-comet` 进行了 AI 重绘
- 保留原角色设定和 SR 统一风格
- 强化玻璃碎星彩片、流星轨迹、冰蓝紫色光晕与星尘层次
- 已同步替换主页面与全部卡片预览页中的卡图映射

## 涉及文件
- `每日打卡.html`
- `全部卡片预览.html`
- `cards/generated/sr-glass-comet/基于输入图进行重绘_保留同一只小马角色_琉璃流星_的核心设定_2026-07-28T10-09-51.png`
- `V1.2-发版说明.md`

## 验收建议
1. 从 `每日打卡.html` 点击“查看全部卡面”
2. 确认先出现口令蒙版
3. 不输入口令时，可点“返回每日打卡”退出
4. 输入正确口令后进入全部卡面预览
5. 刷新页面，确认再次要求输入口令
6. 打开“琉璃流星”与任意卡片详情，确认卡图和底部文案显示正常

## 发布执行说明
当前工作目录下未检测到 `.git` 目录；在此环境中执行 `git status`、`git remote -v` 返回空输出且退出码为 1，因此这里无法直接代你完成实际 commit / push。

如果你本地真实仓库目录就是当前项目，请先确认是在带 `.git` 的仓库根目录下执行下面命令。

## 建议提交信息
`release: 发布 V1.2`

## Push Git 指令
如果你的默认分支是 `main`，直接执行：

```bash
git add "每日打卡.html" "全部卡片预览.html" "V1.2-发版说明.md" "cards/generated/sr-glass-comet/基于输入图进行重绘_保留同一只小马角色_琉璃流星_的核心设定_2026-07-28T10-09-51.png"
git commit -m "release: 发布 V1.2"
git push origin main
```

如果你的默认分支不是 `main`，先查看分支名：

```bash
git branch --show-current
```

然后把最后一行替换成实际分支，例如：

```bash
git push origin master
```

## 发版结论
V1.2 已完成代码整理与发版文档整理。
当前剩余动作仅为：在你的真实 Git 仓库目录执行 commit / push。
