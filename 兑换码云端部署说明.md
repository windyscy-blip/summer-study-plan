# 兑换码同步部署说明

本方案的表面交互是“小朋友输入兑换码，领取一次惊喜奖励”；后台会把这台设备关联到家长账号，并静默同步历史和后续打卡记录。

## 1. 执行数据库脚本

打开 Supabase 项目：

```text
SQL Editor → New query
```

复制并执行项目根目录的 `supabase-schema.sql` 全部内容。

## 2. 开启匿名设备身份

打开：

```text
Authentication → Providers → Anonymous Sign-Ins
```

开启 Anonymous Sign-Ins 并保存。

小朋友设备会使用匿名身份，不需要输入邮箱或密码；该身份仅用于记住兑换成功状态和静默同步。

## 3. 部署兑换码云端函数

安装并登录 Supabase CLI 后，在 Windows CMD 整段执行：

```cmd
cd /d "C:\Users\sPark\WorkBuddy\2026-07-22-15-55-39" && supabase login && supabase link --project-ref lkrcrxycxojfxocxuhcd && supabase functions deploy family-sync
```

函数源码位置：

```text
supabase/functions/family-sync/index.ts
```

该函数负责：

- 家长账号设置或更新 6 位设备兑换码；
- 家长生成、查询、撤销日常奖励码；
- 小朋友端验证兑换码，并由服务端固化奖励结果；
- 将小朋友端打卡快照写到家长账号对应的数据表。

## 4. 设置兑换码

1. 打开线上 `家长查看.html`；
2. 使用家庭账号登录；
3. 在“设备兑换码”填写 6 位数字，例如 `202608`；
4. 点击“保存兑换码”。

设备兑换码只用于新设备首次兑换。更换兑换码不会影响已兑换成功的 iPad。

## 5. 发放日常奖励

1. 打开线上 `家长查看.html` 并登录；
2. 在“日常奖励发放”选择奖励内容、有效期和可选鼓励语；
3. 点击“生成奖励码”，将 6 位数字告诉小朋友；
4. 在“最近奖励记录”中查看领取状态；待领取奖励可以撤销。

日常奖励码每个默认仅可领取一次，奖励结果在生成时由服务端固定，重复提交不会重复发奖。


## 6. 小朋友端使用方式

小朋友端只显示：

```text
输入兑换码 → 立即兑换 → 获得惊喜奖励
```

首次兑换成功后：

- 按设定概率获得 R/SR/SSR 卡或 1/3 星星；
- 该 iPad 后续打卡数据会静默同步；
- 无需显示账号、云端、绑定或家长相关信息；
- 清除 Safari 网站数据后，需要再次输入兑换码。

## 7. 首次关联奖励概率

| 奖励 | 概率 |
|---|---:|
| R 卡 | 60% |
| SR 卡 | 20% |
| SSR 卡 | 5% |
| 3 星星 | 10% |
| 1 星星 | 5% |

奖励结果会保存在该 iPad 的本地奖励账本和云端设备记录中，重复输入或刷新不会重复发奖。

## 8. 上线前检查

1. 数据库脚本已执行；
2. Anonymous Sign-Ins 已开启；
3. `family-sync` 函数已部署；
4. 家长页设置了 6 位兑换码；
5. 小朋友端输入兑换码后显示奖励；
6. 家长页刷新后可看到打卡数据。
