# V1.5 发版说明：日常奖励码

## 本次更新

- 新增家长端“日常奖励发放”：可生成一次性 6 位奖励码。
- 支持发放随机惊喜、1/3/5 星星、随机 R 卡、随机 SR 卡。
- 支持设置当天、3 天或 7 天有效期，并可附加 50 字以内鼓励语。
- 家长端可查看最近 30 条奖励码，显示待领取、已领取、已过期、已撤销状态；待领取的奖励可撤销。
- 小朋友端保留原有“输入兑换码”体验：首次输入设备兑换码完成关联；已关联设备输入日常奖励码即可继续领取奖励。
- 本地奖励账本升级为 V5：用 `claimId` 记录多次兑换，重复请求不会重复获得星星或卡牌；V4 首次兑换奖励会自动迁移保留。
- 日常奖励结果由云端 Edge Function 决定并固化，避免前端刷新或重试导致卡牌不一致。

## 数据库部署（必须）

在 Supabase 项目的 SQL Editor 执行根目录最新 `supabase-schema.sql` 中新增的 V1.5 段落。该段落会新增：

- `reward_codes`：奖励码、奖励结果、有效期、撤销与使用次数；
- `reward_claims`：设备领取账本；
- `claim_reward_code(...)`：带行锁的原子领取函数，防止并发重复领奖。

数据库脚本执行完成前，家长端的“日常奖励发放”无法使用；首次设备关联和打卡同步仍可继续使用。

## Edge Function

`family-sync` 已更新为支持：

- `create_reward_code`
- `list_reward_codes`
- `revoke_reward_code`
- `redeem_reward_code`

部署命令：

```cmd
supabase link --project-ref lkrcrxycxojfxocxuhcd && supabase functions deploy family-sync --project-ref lkrcrxycxojfxocxuhcd
```

## 验证结果

- `test_v15_reward_logic.js` 静态单元测试通过；
- 每日打卡页、家长查看页内联 JavaScript 语法检查通过；
- `check_release.py` 资源与历史 localStorage 迁移检查通过；
- `git diff --check` 通过。

## 已知边界

- 首次设备兑换仍沿用 V1.4 概率，包含 SSR；日常奖励码只开放星星、R、SR 与随机惊喜（随机惊喜仍沿用既定概率）。
- 清除 iPad Safari 网站数据会生成新的匿名设备身份，需要重新使用设备兑换码进行关联。
- 日常奖励码默认单次领取；当前家庭使用模型为单 iPad。
